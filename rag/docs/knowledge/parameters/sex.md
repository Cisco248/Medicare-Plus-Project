# Parameter: sex

Heart-disease model encoded feature: `Sex`. See also gender.md.

The heart-disease e-doc sends `sex` from the profile. Encoding is 1.0 if the text is male, otherwise 0.0. It is used with BMI, smoking, stroke history, diabetes survey status, walking difficulty, general health, physical-health days, and age category.
